--- ext-src/php_swoole_private.h.bak	2025-12-28 23:12:14
+++ ext-src/php_swoole_private.h	2025-12-28 23:12:28
@@ -116,8 +116,8 @@
 
 #if PHP_VERSION_ID < 80100
 #error "require PHP version 8.1 or later"
-#elif PHP_VERSION_ID >= 80500
-#error "require PHP version 8.4 or earlier"
+#elif PHP_VERSION_ID >= 80600
+#error "require PHP version 8.5 or earlier"
 #endif
 
 #if defined(ZTS) && defined(SW_USE_THREAD_CONTEXT)
--- thirdparty/php/main/SAPI.h.bak	2025-12-28 23:13:02
+++ thirdparty/php/main/SAPI.h	2025-12-28 23:13:02
@@ -38,7 +38,7 @@
 
     switch (arg) {
     case PARSE_STRING:
-        separator = PG(arg_separator).input;
+        separator = ZSTR_VAL(PG(arg_separator).input);
         break;
     case PARSE_COOKIE:
         separator = (char *) ";\0";
--- ext-src/swoole_runtime.cc.bak	2025-12-28 23:13:24
+++ ext-src/swoole_runtime.cc	2025-12-28 23:13:24
@@ -199,10 +199,10 @@
             memcpy(new_list, old_list, ZEND_TYPE_LIST_SIZE(old_list->num_types));
             ZEND_TYPE_SET_PTR(new_arg_info[i].type, new_list);
 
-            zend_type *list_type;
+            const zend_type *list_type;
             ZEND_TYPE_LIST_FOREACH(new_list, list_type) {
                 zend_string *name = zend_string_dup(ZEND_TYPE_NAME(*list_type), true);
-                ZEND_TYPE_SET_PTR(*list_type, name);
+                ZEND_TYPE_SET_PTR(*(zend_type *)list_type, name);
             }
             ZEND_TYPE_LIST_FOREACH_END();
         } else if (ZEND_TYPE_HAS_NAME(arg_info[i].type)) {
--- ext-src/swoole_sqlite.cc.bak	2025-12-28 23:14:37
+++ ext-src/swoole_sqlite.cc	2025-12-28 23:14:44
@@ -23,6 +23,11 @@
 #ifdef SW_USE_SQLITE
 using swoole::Coroutine;
 
+#ifndef REGISTER_PDO_CLASS_CONST_LONG
+#define REGISTER_PDO_CLASS_CONST_LONG(const_name, value) \
+    zend_declare_class_constant_long(php_pdo_get_dbh_ce(), const_name, sizeof(const_name)-1, (zend_long)value)
+#endif
+
 static bool swoole_sqlite_blocking = true;
 
 void swoole_sqlite_set_blocking(bool blocking) {
--- ext-src/swoole_odbc.cc.bak	2025-12-28 23:14:53
+++ ext-src/swoole_odbc.cc	2025-12-28 23:15:01
@@ -24,6 +24,11 @@
 
 #ifdef SW_USE_ODBC
 
+#ifndef REGISTER_PDO_CLASS_CONST_LONG
+#define REGISTER_PDO_CLASS_CONST_LONG(const_name, value) \
+    zend_declare_class_constant_long(php_pdo_get_dbh_ce(), const_name, sizeof(const_name)-1, (zend_long)value)
+#endif
+
 static bool swoole_odbc_blocking = true;
 
 #ifdef SQL_ATTR_CONNECTION_POOLING
--- ext-src/swoole_oracle.cc.bak	2025-12-28 23:14:53
+++ ext-src/swoole_oracle.cc	2025-12-28 23:15:08
@@ -22,6 +22,11 @@
 
 #ifdef SW_USE_ORACLE
 
+#ifndef REGISTER_PDO_CLASS_CONST_LONG
+#define REGISTER_PDO_CLASS_CONST_LONG(const_name, value) \
+    zend_declare_class_constant_long(php_pdo_get_dbh_ce(), const_name, sizeof(const_name)-1, (zend_long)value)
+#endif
+
 static bool swoole_oracle_blocking = true;
 
 void swoole_oracle_set_blocking(bool blocking) {
--- ext-src/swoole_pgsql.cc.bak	2025-12-28 23:14:53
+++ ext-src/swoole_pgsql.cc	2025-12-28 23:15:14
@@ -23,6 +23,11 @@
 
 #ifdef SW_USE_PGSQL
 
+#ifndef REGISTER_PDO_CLASS_CONST_LONG
+#define REGISTER_PDO_CLASS_CONST_LONG(const_name, value) \
+    zend_declare_class_constant_long(php_pdo_get_dbh_ce(), const_name, sizeof(const_name)-1, (zend_long)value)
+#endif
+
 using swoole::Coroutine;
 using swoole::EventType;
 using swoole::Reactor;

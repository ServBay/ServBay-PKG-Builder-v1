--- ./apc_cache.c.bak	2025-11-06 12:51:48
+++ ./apc_cache.c	2025-11-06 12:51:48
@@ -613,7 +613,7 @@
 				apc_cache_store(
 					cache, name, &data, 0, 1);
 				zend_string_release(name);
-				zval_dtor(&data);
+				zval_ptr_dtor_nogc(&data);
 			}
 			return 1;
 		}
--- apc_persist.c.bak	2024-09-22 03:08:07
+++ apc_persist.c	2026-04-27 21:07:29
@@ -17,6 +17,21 @@
  */
 
 #include "apc.h"
+#ifndef INI_STR
+# define INI_STR(name) zend_ini_string((name), (size_t)strlen(name), 0)
+#endif
+#ifndef INI_INT
+# define INI_INT(name) zend_ini_long((name), (size_t)strlen(name), 0)
+#endif
+#ifndef INI_FLT
+# define INI_FLT(name) zend_ini_double((name), (size_t)strlen(name), 0)
+#endif
+#ifndef INI_BOOL
+# define INI_BOOL(name) zend_ini_parse_bool(zend_ini_str((name), (size_t)strlen(name), 0))
+#endif
+#ifndef EMPTY_SWITCH_DEFAULT_CASE
+# define EMPTY_SWITCH_DEFAULT_CASE() default: ZEND_UNREACHABLE();
+#endif
 #include "apc_cache.h"
 
 #if PHP_VERSION_ID < 70300

--- sql/auth/sql_authorization.cc.bak   2024-07-15 16:10:02
+++ sql/auth/sql_authorization.cc       2024-07-15 16:36:06
@@ -7429,6 +7429,12 @@
       if (ptr->has_executed_drop_policy()) delete ptr;
     }
   });
+}
+
+bool operator==(const Role_id &a, const LEX_CSTRING &b) {
+    std::string tmp;
+    a.auth_str(&tmp);
+    return tmp == std::string(b.str, b.length);
 }

 bool operator==(const Role_id &a, const std::string &b) {